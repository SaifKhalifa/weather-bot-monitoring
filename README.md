# weather-bot-monitoring
A C# console application that simulates a real-time weather monitoring and reporting system using configurable bots (RainBot, SunBot, SnowBot). It supports input in JSON and XML formats and uses SOLID principles with Strategy and Observer patterns.
